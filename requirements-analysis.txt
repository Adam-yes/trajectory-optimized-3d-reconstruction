# Pinned versions used for the recorded validation run.
# Tested on Python 3.13.5; Python 3.11+ is recommended for these pins.
# The package itself declares compatibility ranges in pyproject.toml.
numpy==2.3.5
scipy==1.17.0
Pillow==12.3.0
matplotlib==3.10.8
